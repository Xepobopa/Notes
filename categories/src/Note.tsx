import React from 'react';
import {useNote} from "./NoteLayout";

const Note = () => {
    const note = useNote();

    return (
        <div>
            
        </div>
    );
};

export default Note;